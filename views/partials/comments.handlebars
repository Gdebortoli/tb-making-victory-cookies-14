<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} posted 
    </div>
    <div class="text">
      {{comment_text}} on {{format_date created_at}}
    </div>
  </section>
  {{/each}}
</div>